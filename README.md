# azure-direct-accounting
This is a simple of implementing token-based authentication using Azure OpenID Connect with a .Net Core API-based backend and an Angular-based frontend, where the API handles token exchange, and on the frontend
